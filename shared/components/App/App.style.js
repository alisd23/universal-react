import { style } from 'glamor';
import * as colors from 'shared/styles/colors.style';

export const content = style({
  padding: 30,
  paddingTop: 10,
  marginBottom: 20,
  backgroundColor: colors.grey.lightest
});

export const root = style({
  padding: 20
});
